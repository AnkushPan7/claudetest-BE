MCP tool, the backend sometimes returns errors (e.g, "Order not found" or temporary database failures). What is the correct
When implementing your lookup_order
pattern for communicating these errors back to the agent?
O
Log the error server-side and return an empty result to avoid confusing the model
O
o
Return a success response with a "status" field indicating the error type
Return the error message in the tool result content with the isError
flag set to true
O
o
Throw an exception from the tool handler so the agent framework can catch and log it
